# 20: specification synchronization

Feature behavior belongs in `docs/features/`; screen behavior in `docs/screens/`; entities in `docs/DATABASE.md`; endpoints in `docs/API_SPEC.md`; cross-cutting decisions in `docs/DECISIONS.md`; acceptance in `docs/acceptance/` and `docs/TESTING.md`.

Update all affected authoritative documents in the same change. Keep `docs/TRACEABILITY.md` complete. State preparation, implementation and live provider evidence accurately. Use plain Markdown tables/prose for pre-onsite contracts; do not introduce executable schema or application snippets.
